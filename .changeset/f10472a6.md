---
"hex-app": patch
---

Cloud Sync hardening: fix GCS path encoding (download/delete now work cross-device), debounce per-note uploads to prevent racing PATCHes, use stable per-install device ID, clean up orphaned photos when bodies are edited, and trigger sync on scenePhase active rather than at launch.
