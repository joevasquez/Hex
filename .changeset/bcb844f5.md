---
"hex-app": patch
---

Settings reorganization: Recording tab is now the comprehensive recording hub (model, mic, hotkeys, during-recording behavior, output, history). General tab slimmed to permissions + sound + app toggles. AI tab restructured into Provider / Default Mode / Behavior subsections. Sidebar gets a min-width and slightly polished pill toggle so Settings/History buttons no longer get crushed at narrow widths.
