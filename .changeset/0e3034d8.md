---
"hex-app": patch
---

AI post-processing no longer treats the transcript as a conversation — wraps user content in <transcript> tags and falls back to raw text when the model still refuses. Also fixes Email mode emitting a literal "<Your name>" placeholder.
